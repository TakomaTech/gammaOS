# tools

Userland tools and utilities for Gamma kernel development.

This directory is for development tools, debugging helpers, and kernel build utilities.

## Available tooling

- `run-qemu.sh` — launches the latest `build/gamma.iso` in QEMU.

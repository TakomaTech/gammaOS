# scripts

This directory holds build and development scripts for the Gamma kernel.

Common script categories:
- build helpers
- kernel configuration
- tooling wrappers

## Available scripts

- `build.sh` — invoke the repository `Makefile` from the project root.
